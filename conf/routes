# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Security Test routes

# Public routes
GET     /health          controllers.HealthController.health()

# Secure routes - will require JWT
GET     /secure-health   controllers.HealthController.secureHealth(request: Request)

# An example controller showing a sample home page
GET     /auth/logout                controllers.AuthController.logout(request: Request)

#GET     /files-signed              controllers.FilesController.s3Signed(key: String)
GET     /files-s3-thumbnail        controllers.FilesController.s3Thumb(key: String)

GET     /fin-insts                      controllers.admin.FinancialInstitutionController.index(request: Request)
GET     /fin-insts/create               controllers.admin.FinancialInstitutionController.createNewFinInst(request: Request)
GET     /fin-insts/view/:hashedId       controllers.admin.FinancialInstitutionController.viewInstitute(hashedId: String, request: Request)
GET     /fin-insts/edit/:hashedId       controllers.admin.FinancialInstitutionController.editInstitute(hashedId: String, request: Request)
GET     /fin-insts/delete/:hashedId     controllers.admin.FinancialInstitutionController.deleteInstitute(hashedId: String, request: Request)
GET     /fin-insts/save-redirect/     controllers.admin.FinancialInstitutionController.saveAndRedirectToConfig(request: Request)
GET     /fin-insts/update-redirect/     controllers.admin.FinancialInstitutionController.updateAndRedirectToConfig(hashedId: String, request: Request)


GET     /domains/edit/:hashedDomainType/:hashedDominId       controllers.admin.DomainRouterController.editDomain(hashedDomainType: String, hashedDominId: String)
GET     /domains/:hashedDomainType     controllers.admin.DomainRouterController.domainIndex(hashedDomainType: String)


GET     /domains/endpoint-configs/:hashedDomainType/:hashedDominId       controllers.admin.EndpointConfigController.showForm(hashedDomainType: String, hashedDominId: String, request: Request)
GET     /domains/endpoint-configs/save     controllers.admin.EndpointConfigController.save(request: Request)


POST     /save-new-fin-inst              controllers.admin.FinancialInstitutionController.saveNewFinInst(request: Request)
POST     /fin-insts/update/:hashedId     controllers.admin.FinancialInstitutionController.updateOldFinInst(hashedId: String, request: Request)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
